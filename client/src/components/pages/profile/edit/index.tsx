import React, { FC } from 'react';
import Profile from '../index';
import EditLayout from '../../../layouts/profileLayout/editLayout';

const Edit: FC = () => {
  return (
    <Profile>
      <EditLayout />
    </Profile>
  );
};

export default Edit;